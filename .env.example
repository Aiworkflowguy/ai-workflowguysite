# Copy this file to `.env` and fill in your key. `.env` is gitignored.
# Free key: https://aistudio.google.com/apikey
# Used only by `npm run images` (AI image generation). Not needed to build/deploy.
GEMINI_API_KEY=
