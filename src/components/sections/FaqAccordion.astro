---
import { faqs } from '../../config/faqs';
---
<section class="section faq">
  <div class="container faq__wrap">
    <div class="reveal">
      <span class="eyebrow">FAQ</span>
      <h2 class="h2" style="margin-top:.8rem">Questions, answered plainly.</h2>
    </div>
    <div class="faq__list reveal">
      {faqs.map((f) => (
        <details class="faq__item">
          <summary>{f.q}</summary>
          <p class="muted">{f.a}</p>
        </details>
      ))}
    </div>
  </div>
</section>

<style>
  .faq__wrap { display: grid; gap: 2rem; grid-template-columns: 1fr; }
  .faq__list { display: grid; gap: .7rem; }
  .faq__item { border: 1px solid var(--border); border-radius: var(--radius-sm); padding: 0 1.1rem; background: var(--surface); }
  .faq__item summary { cursor: pointer; padding: 1.05rem 0; font-family: var(--font-display); font-weight: 500; list-style: none; display: flex; justify-content: space-between; gap: 1rem; }
  .faq__item summary::-webkit-details-marker { display: none; }
  .faq__item summary::after { content: "+"; color: var(--lime); font-family: var(--font-mono); }
  .faq__item[open] summary::after { content: "–"; }
  .faq__item p { padding: 0 0 1.1rem; }
  @media (min-width: 860px) { .faq__wrap { grid-template-columns: .8fr 1.2fr; } }
</style>
